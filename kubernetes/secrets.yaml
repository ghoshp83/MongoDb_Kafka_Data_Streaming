apiVersion: v1
kind: Secret
metadata:
  name: mongodb-credentials
type: Opaque
data:
  # Base64 encoded MongoDB URI - MUST be replaced with actual credentials
  # Example: echo -n "mongodb://user:password@host:port/db" | base64
  # This placeholder will cause connection failures - replace before deployment
  uri: ""
